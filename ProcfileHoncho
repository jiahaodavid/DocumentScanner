node: cd frontend && npm install && yarn start
python: cd backend && pip install -r requirements.txt && python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt && gunicorn --bind 0.0.0.0:5000 main:app"